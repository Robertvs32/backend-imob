import { Usuario } from "./usuario.type";

const UsuarioService = {

    cadastrarUsuario: async (dadosUsuario: Usuario) => {
        // verifica se o usuario ja existe
    }

}

export default UsuarioService;